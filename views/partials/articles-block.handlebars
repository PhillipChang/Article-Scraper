
    <div class="col s4 m3">
      <div class="card blue-grey darken-1">
        <div class="card-content white-text">
          <span class="card-title">{{title}}</span>
          <p data-id="{{_id}}" data-title="{{title}}" data-link="{{link}}">{{link}}</p>
        </div>
        <div class="card-action">
          <a href="#">Add Note</a>
          <a href="#">Save Note</a>
        </div>
      </div>
    </div>
 

